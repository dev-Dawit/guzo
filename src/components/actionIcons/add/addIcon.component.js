import React from 'react';
import AddIcon from '@mui/icons-material/Add';

export const AddNewIcon = ({ onClick }) => {
  return <AddIcon onClick={onClick} style={{ color: '#1890ff', cursor: 'pointer', marginBottom: '2px' }} />;
};


