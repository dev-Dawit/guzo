import React from 'react';
import ListAltIcon from '@mui/icons-material/ListAlt';

export const ListIcon = ({ onClick }) => {
  return <ListAltIcon onClick={onClick} style={{ color: '#1890ff', cursor: 'pointer', marginBottom: '2px' }} />;
};


